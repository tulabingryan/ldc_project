# Copy the following files to boot drive of the raspberry pi.

"ssh"
"wpa_supplicant.config"

# Upon boot up, the raspi will automatically change its network configuration
# and connect to the available wifi network according to the priority set in the supplicant file 

